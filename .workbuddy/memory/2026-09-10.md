# 2026-09-10

- 完成 M10C 3.6.0 发布卡片出图：`release-cards` 页面（中文 6 条 / 英文 5 条）经 snapdom 导出 6 张 1080×1440 PNG，归档到 `screenshots/release/v3.6.0/`，命名 `{lang}-{NN}-{kind}.png`（zh/en × cover/updates/install）。
  - MCP Chrome 的下载目录就是 **`~/Downloads`**，snapdom 落盘文件名 = `data-card` 值且**无扩展名**。
  - `cut -d-` 字段：1=m10c、2=版本(3.6.0)、3=lang、4=序号、5=kind（版本号含点不含连字符，别数错）。
  - 目视核对：logo 在位、强调色 `#A18BFF` 正确、四角为纸张色 `#F4F2ED`、无截断。
- 英文版改动：CHANGELOG 第 6 条（Six-locale cleanup）删除、封面数量改「5 updates」、install 标题压成「New version / is live.」；中文版保持 6 条不变（内容计数中英不一致，如需对齐告知）。
- 新增项目级技能 `.workbuddy/skills/release-cards/SKILL.md`，记录发新版三步 + 中英溢出约束 + 导出归档流程（含上述 cut 字段坑）。

## 20:25 追加修订
- 用户指出中文第六条也该删（之前只在英文删）→ zh 列表去掉「六语言文案清理」，封面数量改「更新 5 项」，中英现均为 5 条；重导 zh-01 / zh-02。
- 英文标签胶囊宽度不齐（NEW vs IMPROVED）→ `.ls-item .tg` 改为按语言固定宽度 + 居中：`body[data-lang="en"]` 80px、`zh` 44px，标题列现在严格左对齐；重导 en-02。
- 踩坑 1：改了 JSON 页面不变 —— 浏览器对 JSON 用了启发式缓存。修复：`fetch(..., {cache:'no-store'})`（index.html 已加），已写进技能。
- 踩坑 2：重复导出时 Chrome 把文件存成 `m10c-3.6 (1).0-xx`（` (1)` 插在第一个点后），因为源文件名无扩展名。归档前先 `ls -lt ~/Downloads` 确认实际名字。已写进技能。
- 另有 3 张未变动的图（zh-03 / en-01 / en-03）直接沿用首批导出。
- 出图后清理：`~/Downloads` 里 9 个无扩展名导出文件已 `mv` 到 `~/.Trash`（保留 6 月的 m10c.png，非本次产物）。流程第 7 步已补进 `.workbuddy/skills/release-cards/SKILL.md`（清前核对归档大小一致、用 Trash 不用 rm）。
- `screenshots/` 已加入 `.gitignore`（生成产物不入版本管理），并 `git rm -r --cached screenshots` 取消已暂存的 6 张图 —— 本地文件保留，索引中移除。以后发版出图无需手动排除。
