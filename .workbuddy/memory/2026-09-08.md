# 2026-09-08

- 在 README.md / README.zh.md 新增「应用商店描述」章节（可直接复制到 Chrome/Firefox/Edge 商店）。
- **重要项目事实（曾写错后修正）**：「已生成内容免费获取」的真实逻辑（依据 docs/free-generation-quota-requirements.md 与 MindmapDisplay.tsx）：
  - 服务端缓存获取 `GET /api/v1/mindmap/fetch`：**仅限走内置托管模型（星星算力，非 BYOK）生成的内容**才有云端缓存；此类已生成内容可直接免费领取现成结果，不消耗星星、不占免费生成额度；每人每月限 5 次免费获取（`video_mindmap_quotas`），超限提示消耗星星重新生成。自带 API Key（BYOK）生成的内容不上云、无此福利，仅本地缓存。
  - 免费生成额度（freeGeneration campaigns）是另一套活动赠送额度，只用于生成新内容；缓存获取永远优先于免费生成（`X-Free-Generation: true` 头标记免费生成）。
  - 本地缓存 `loadCacheData` 有 24 小时过期。
- 学习并应用了 https://github.com/coreyhaines31/marketingskills 的 `copywriting` + `product-marketing` 技能（v2.x），按其原则重写了两份 README 的商店描述：
  - 核心原则：清晰>花哨、收益>功能、具体>空泛、用户语言>公司语言、开头用痛点场景钩子（如"40 分钟的视频你只需要 3 个重点"）、结尾给行动指令（CTA）。
  - 中文重写版开头钩子："一个 40 分钟的视频，你需要的可能只是 3 个重点"；每条特性改为收益导向标题（"视频：先看总结再决定看不看"）。
  - 该仓库是通用营销技能库（50 个技能），base skill 为 product-marketing（建议落盘 `.agents/product-marketing.md` 供其他技能引用），后续做落地页/推广文案（local-docs/promotion-*）时可复用。
- **AI 供应商列表 + 多模型配置改造**（参考 mind-elixir-desktop 的 sync-ai-providers 技能与 AISettings.tsx）：
  - 新增 `scripts/sync-providers.mjs`（models.dev 同步：KNOWN_API_BASE_URLS 补 Base URL、按 baseURL 去重、过滤无 api 的 SDK 专用渠道），输出 `config/models-dev-providers.json`（194 家，36KB）；`pnpm update:providers` 一键更新。
  - 新增 `utils/ai-providers.ts`：models.dev 数据解析层（getProviderConfig/getProviderLabel/getProviderBaseUrl/getProviderOptions/fetchOpenAICompatibleModels），CUSTOM_PROVIDER_ID="openai-compatible"，LEGACY_PROVIDER_BASE_URLS 兼容旧 gemini/claude/openrouter/openai id。
  - `utils/ai-service.ts` 升级为 V3 多模型结构（`local:aiConfigV3`：models[] + defaultModelId + replyLanguage），内置 Mind Elixir 模型永远首位（createBuiltInModel/isMindElixirModel），读时自动迁移 V2 并回写。
  - `entrypoints/options/App.tsx` 重写：模型列表 RadioGroup 选默认、新增/编辑/复制/删除表单、可搜索供应商下拉（190+，无 cmdk 自实现）、Base URL 只读保护、模型列表自动拉取；Mind Elixir 登录/星星余额面板仅默认模型为内置时显示。
  - background/prompts/popup/SubtitlePanel 全部改走 `loadAIModelsConfig()`；background 协议路由：gemini/claude 走旧原生 handler，其余（含全部 models.dev 供应商）走 OpenAI 兼容。
  - 6 个 locale 各补 11 个 i18n key；tsc + wxt build 全过，JSON 已确认打包进 background.js。

## 2026-09-08
- 修复设置页模型列表星星过多：移除内置模型名称与 provider label 中的 ⭐（ai-service.ts `createBuiltInModel`、ai-providers.ts `getProviderLabel`），保留列表项的 Star 图标；`loadAIModelsConfig` 对存量配置做名称规范化。
- 优化 MINDMAP_SYSTEM 提示词（entrypoints/background/prompts.ts）：修复规则 3（每行必须以 "- " 开头）与规则 7（关系行示例无 "- " 前缀）的自相矛盾——新增 UNIFIED PREFIX RULE 统一所有行前缀，明确 summary/relation 行形态与空格要求（"}" 后必须一个空格）、禁止标记混用（如 "}> "），规则 7 示例补上 "- " 前缀并要求关系行独立成行、引用已定义的 [^id]，末尾新增 Common mistakes 反例清单（缺 "- " 前缀 / "}> " 混用 / "}" 后缺空格 / summary 与 relation 混行）。背景：某模型输出 "- }> [^id] <-label-> [^id]" 杂交语法 + "}" 后丢空格，根因是提示词矛盾而非模型完全没看懂。用户明确只改 prompt，解析端容错未做（后续可选：解析前归一化 "- }>" → ">"、"}" 后补空格）。
- 页面内 panel 品牌化：给两个插入页面的面板（components/SubtitlePanel.tsx 视频助手、entrypoints/article-mindmap.content.tsx 文章助手）标题左侧加 m10c logo。做法：`sips` 将 assets/icon.png（210KB/1023px）缩为 assets/logo-64.png（4.4KB），两处以 `import brandLogoUrl from "~/assets/logo-64.png?inline"` 引入——`?inline` 强制 base64 data URL，Shadow Root 内直接可用，无需 web_accessible_resources；样式 w-[20px] rounded-[5px]。tsc + wxt build 均过，已确认 base64 进入 3 个 content-scripts 产物。
- 按用户要求去掉 6 个 locale 中 `articleAssistant` 文案前缀的 📄 emoji（public/_locales/*/messages.json），构建产物已验证。
